version: 2.1
jobs:
   build:
      docker: 
        - image: circleci/node:13.8.0
      steps:
        - checkout
        - run: npm init -y
        - run: npm run lint

workflows:
   webapp:
      jobs: 
        - build 


